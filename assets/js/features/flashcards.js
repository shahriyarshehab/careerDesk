/* ==========================================================================
   CareerDesk — Flashcards Module (Deprecated & Replaced by MCQ Engine)
   Retained as backward-compatible safe stubs to prevent reference errors.
   ========================================================================== */

function renderFlashCategoryOptions() {
  // Safe no-op
}

function renderFlashcards() {
  // Safe no-op
}

function openFlashcardExam() {
  // Safe no-op
}

function exitFlashcardExam() {
  // Safe no-op
}
